<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.findmypet">

    <uses-feature
        android:name="android.hardware.CAMERA"
        android:required="false" />

    <uses-permission android:name="android.hardware.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".dal.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_principal"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_principal_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".AdicionarComentarioActivity"></activity>
        <activity android:name=".PerfilActivity" />
        <activity android:name=".Formularios.CadastroActivity" />
        <activity
            android:name=".Formularios.Login"
            tools:ignore="InnerclassSeparator" />
        <activity
            android:name=".Formularios.FormularioPost"
            tools:ignore="InnerclassSeparator" />
        <activity android:name=".DetalheAnimal" />
        <activity
            android:name=".TelaAbertura"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>